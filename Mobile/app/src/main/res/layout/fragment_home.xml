<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Home">

    <com.google.android.gms.maps.MapView
        android:id="@+id/id_map"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- Floating logo dengan margin yang lebih rapi -->
    <ImageView
        android:id="@+id/floatingLogo"
        android:layout_width="140dp"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:contentDescription="@string/logo_desc"
        android:src="@drawable/logo"
        android:scaleType="fitCenter"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"/>

    <!-- Center to Location FAB (Premium Refactor) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="24dp"
        android:contentDescription="Center Camera"
        android:src="@android:drawable/ic_menu_mylocation"
        app:backgroundTint="#2A2A2A"
        app:tint="#8194FF"
        app:fabSize="normal"
        app:elevation="8dp"
        app:rippleColor="#408194FF"
        app:borderWidth="0dp"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Full" />

</RelativeLayout>
